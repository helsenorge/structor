import React from 'react';
import ListView from '../../PatientInfo/ListView/ListView';

const SchemaView = () => {
    return (
        <>
            <ListView />
        </>
    );
};

export default SchemaView;
