export enum ValidationType {
  dataReceiver = "data-receiver",
  table = "table",
  security = "security",
  sidebar = "sidebar",
  endpoint = "endpoint",
  binary = "binary",
  readonly = "readonly",
  mandatory = "mandatory",
}

export enum ErrorLevel {
  warning = "warning",
  info = "info",
  error = "error",
}
