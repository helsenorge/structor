export enum ValidationType {
  dataReceiver = "data-receiver",
  table = "table",
  security = "security",
  sidebar = "sidebar",
  endpoint = "endpoint",
  binary = "binary",
}

export enum ErrorLevel {
  warning = "warning",
  info = "info",
  error = "error",
}
